baseURL = 'https://romanthapa.com/'
languageCode = 'en-us'
title = 'Roman Thapa'
theme = 'roman'

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true  # This allows raw HTML to be rendered in markdown
    [markup.goldmark.extensions]
      typographer = true  # This enables smart punctuation like curly quotes
